# 执行台账与运行保障｜2026-09-09

状态：已完成本地验证；工作流修复尚未提交或部署。数据与版本以 2026-09-09 的仓库、线上 HTML 和已读取的平台数据为准。

## 变更观察台账

| 页面 | 线上标题 | 最后内容提交 | 线上状态 | 观察规则 |
| --- | --- | --- | --- | --- |
| `/daily/` | 日替わりナンプレ｜初級2問を無料で・今日の全7問 - numpredo | `eb04f71`，2026-09-08 02:30 +08 | 200，自引用 canonical；线上覆盖 9/8～11/6 | 9/25 初评，10/9 完整 28 天复盘；按查询簇、设备、Google/Bing 和有效任务拆分 |
| `/guide/sudoku-vs-numpre/` | 数独とナンプレの違いは呼び方｜ルールは同じ・名前の由来を比較 - numpredo | `8780f09`，2026-09-08 04:10 +08 | 200，自引用 canonical | 同上；直接答案型 SERP 不以整页 CTR 单独判定 |

名称区别页还经历 `908d551`（02:40）、`e2ce2b5`（02:58）、`b63d17b`（03:29）和 `8780f09`（04:10）四次连续提交。观察窗从最后一次实际线上版本开始；Git 时间不能替代部署时间。

## 自动变更来源与运行保障

唯一确认的持续自动改动来源是 `.github/workflows/daily-build.yml`：它只生成 `sitemap-lastmod.json` 和 `published.json`，不改页面 title、description 或正文。

- 失败运行 `34155793527`：2026-09-07 的 `git push` 因远端 main 在运行期间新增内容提交而拒绝，未覆盖远端内容。
- 后续成功：远端 `dca66dd`（2026-09-08 19:10 UTC）更新了日次数据，说明题库与生成链没有故障。
- 本地修复：每次推送前抓取并重置 Actions 临时 checkout 到 `origin/main`，重新生成两个派生文件；推送冲突时最多三次重试。没有 rebase 生成文件、force push 或修改题库顺序。
- 仍待关闭的证据：Cloudflare Pages 的实际部署 SHA 无法读取；线上 daily 和归档目前均为 200，但这不替代平台部署记录。

## 测量口径

GA4 自然渠道 `(not set)` 的主要成因是回访用户在长期打开页面上再次操作，形成新 session 但没有新 page_view。当前不补发 page_view。`game_complete`、`solver_solve`、`print_pdf_download` 已作为每 session 一次的 key event；自然渠道的任务衡量使用 session key event，不用 `game_complete / game_start` 计算完成率。

## 2026-09-10：自然渠道任务衡量已关闭

- 已按 GA4 `eventName × sessionDefaultChannelGroup` 固定自然任务会话基线（08/12～09/08，Asia/Tokyo）：玩题完成 1,709、求解 337、PDF 下载 334；事件会话分别报告，禁止相加或以 `game_complete / game_start` 当完成率。
- 已回读 `sudoku-game`、`ocr-solve`、`print` 与公共 `track` 实现；未发现重复或错误触发证据，因此不改前端埋点，也不补发 `page_view`。
- 详细事件字典、事件次数/用户数、风险和复核条件见 [自然渠道任务衡量口径｜2026-09-10](2026-09-10-自然渠道任务衡量口径.md)。

## 核心路径回归

本地生产构建（390px 视口）验证：

- solver：空盘提示、样例加载、唯一解（30 输入格 + 51 解答格）、6 个步骤图解、行内重复冲突提示均正常。
- 高龄者打印：默认“初级”“2问”“大きく2問”配置生效，点击生成后出现两题及解答；`numpredo-senior-12.pdf`、`numpredo-senior-60.pdf` 均返回 200。
- PDF 资源：12 个文件均存在；12 题为 8 页，60 题为 40 页，均为 PDF 1.4。
- 自动验证已通过：`npm run check:daily`、`npm run check`、`npm run demo`、`npm run build`。

下一项仅在当前工作流修复发布后继续：观察 daily 的下一次日次运行与线上窗口；不等号完整例题或学习动线只在发现明确的用户任务断点后作最小改动。
