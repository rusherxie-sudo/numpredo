# numpredo：项目现状与接管基线

核验日期：2026-09-09。代码基线：main，`8780f0922b7a9bed070ccc0848323775f3815438`。本轮重新检查源代码、题库、构建产物、线上页面、Git 历史及第一方搜索/行为数据，只重建本文与 GROWTH.md。

## 工作原则与文档职责

AGENTS.md 是本项目的首要工作原则：日本数独游戏站点；技术服务于自然搜索增长与用户价值；历史实现只作为证据。重大工作必须说明增长收益，按“小范围验证，再扩大”推进。已有 URL、索引资产和生产数据须谨慎处理；大量 URL 变化、页面删除、数据迁移、DNS 和生产数据库破坏性操作不得直接执行。

本文描述可核验的接管状态；[GROWTH.md](GROWTH.md) 负责策略，[BASELINE.md](BASELINE.md) 维护详细搜索/增长基线、后续线上补查与最新 30 天优先级及验收标准。CLAUDE.md、TASKS.md、docs/ 和引擎 README 是历史材料，不自动成为当前路线图。发现与本文不符时，以当前代码、线上状态和有日期范围的数据修订本文。

接管前工作区已有 .gitignore 修改与未跟踪的 AGENTS.md，本轮保留。未修改业务代码、配置、题库、重定向、远端账户或生产部署；构建仅更新本地忽略的产物。

## 产品真实定位

域名为 https://numpredo.com，产品语言日语，主要面向日本用户，开发沟通用中文。当前价值是让用户在浏览器内免费、无需注册地完成“玩一题、解决卡住的题、学习具体手筋、下载并打印题目”四类任务。

这已经是有工具和自有题库支撑的数独产品，不能再用旧文档“约 80% 静态内容页”决定投入。技术上静态生成 HTML，不代表用户价值以文章为主。Google 最大入口是求解器；Bing 和 GA4 同时证明在线游戏、难度页、每日题和打印的价值。

| 用户/场景 | 搜索意图 | 现有承接资产 |
| --- | --- | --- |
| 日本休闲玩家与常玩用户 | 免费在线数独、按难度玩、每日题 | /play/、五档难度、/daily/、归档 |
| 手持纸题/截图、遇到卡点的用户 | 自动解答、照片/相机识别、下一步 | /tools/solver/、候选检查器、16×16 求解器 |
| 学习与进阶用户 | 解法、手筋、难题、变体规则 | 指南、技巧、例题图解、练习工具 |
| 打印使用者、家属、教师及活动组织者 | 免费 PDF、答案、大字、空白格 | 打印工具、五档打印页、高龄者页、空白模板 |

细分受众来自查询与产品用途；没有年龄、职业画像数据，不能把场景假设写成实际人口统计。GSC 本期日本点击占 99.2%，日本市场定位得到直接支持。

## 当前页面规模

重新构建得到 **64 个 HTML 产物：63 个目录页面和一个 404 页面**。其中 /stats/ 为 noindex,follow，Sitemap 列出 **62 个可索引 URL**。线上 Sitemap 与本地 URL 集合一致，62 个线上 URL 全部返回 200、自引用 canonical 且未设置 noindex。

| 页面组 | 可索引 URL 数 | 说明 |
| --- | ---: | --- |
| /guide/ | 28 | Hub、15 篇数据驱动指南、10 篇技巧、解法总览、5 档例题集 |
| /play/ | 6 | Hub + 五档；各难度页固定提供 90 道可切换题 |
| /variants/ | 7 | Hub + 4×4、6×6、不等号、16×16、对角线、Killer |
| /print/ | 9 | Hub + 五档 + 高龄者 + 空白格 + 技巧练习 |
| /tools/ | 3 | 标准求解器、生成器、候选检查器；没有独立 tools Hub |
| /daily/ | 2 | 每日题和归档；不是一天一个独立索引 URL |
| 其他 | 7 | 首页、练习、研究、关于、联系、隐私、条款 |

可索引数不是 Google 收录数。本期 GSC 中，现有 62 个 URL 的 61 个出现过曝光、56 个出现过点击；这只能证明窗口内搜索表现，不能代替当下逐 URL 索引检查。

## 核心资产与数据源

### 题库与引擎

- 标准 9×9 共 **4,395 题**：beginner/intermediate/advanced/extreme 各 1,000，hard 395。每题保存题面与解，提交在 src/data/puzzles/。
- daily.json 也有 4,395 条，是既有题库的派生集合，当前页面不消费它；仍用于 demo.ts 溯源复核，不能再次计入原创题量。
- 另有对角线 30 题、Killer 30 题、src/data/mini4.ts 中 4×4 12 题。16×16 有独立求解/示例能力，不属于标准题统计。
- src/engine/ 是框架无关 TypeScript：候选位掩码、约束上下文、唯一解检查、逻辑求解、难度分类、生成、SVG 和步骤图解。标准逻辑链有 9 个技巧；教程有 10 篇，不代表引擎实现了全部教程技巧，例如 XY-Wing 教程存在而标准技巧链未实现它。
- 难度主要按实际求解中最难技巧判定，而非仅按空格数。标准题验证是唯一解、逻辑可解、逻辑解与存储答案一致。
- 题库生成需要保护既有前缀与顺序：每日题、存档、打印和题号引用依赖它们。题库在离线生产，不是每次访问重新生成全部数据。

### 工具、内容与分发资产

标准游戏支持进度保存、候选、撤销、提示、计时、统计与实绩；记录主要保存在浏览器 localStorage，没有账户同步。标准求解器支持手动输入、粘贴、照片 OCR、解答与步骤图解。OCR 在浏览器按需加载外部 tesseract.js@5.1.1，因此“没有业务后端”不等于“没有外部依赖”。

4×4、对角线和 Killer 有交互盘面，16×16 有专用组件；6×6 和不等号目前主要是规则/解法内容页，不能把所有变体都描述为完整在线游戏。

public/downloads/ 有 12 个预制 PDF（五档 + 高龄者，各 12 题/60 题），另有浏览器打印工具。PDF 由 Python/ReportLab 脚本生成。研究页提供标准 4,395 题的逐题 CSV 和可引用 PNG，属于自有题库分析，不代表行业总体研究。指南、例题和技巧图解把求解能力转化为可抓取内容。

## 技术结构

| 层 | 当前实现与位置 | 运维含义 |
| --- | --- | --- |
| 页面 | Astro 7.2.6，纯静态输出；src/pages/ | 文件及 getStaticPaths 生成路由；无业务 API/数据库 |
| 共享层 | src/layouts/Base.astro、src/styles/tokens.css | 日语 lang、导航、页脚、canonical、社交元信息、统计 |
| 内容 | guides.ts、techniques.ts、variants.ts、levels.ts、print-levels.ts | 内容位于 TS 数据对象与 Astro 模板；没有 CMS |
| 交互 | src/components/ + src/client/ | 原生浏览器 TypeScript，不是 React 应用 |
| 数据/派生物 | JSON 题库、PDF、分析 CSV/PNG、SVG、llms 文件 | 多份派生物须同步验证，不能仅改数量标签 |
| SEO 时间 | published.json、sitemap-lastmod.json | 离线从 Git 与内容映射生成；构建只读，不自动等于构建日期 |
| 构建 | npm、TypeScript 5.9、Astro、sitemap、sharp | package 要求 Node ≥22.12；CI 配置 Node 24，本轮本机为 26.8.1 |

核心数据流：离线生成并复核题库 → 提交 JSON → Astro 构建页面/图解与客户端数据 → 浏览器游戏和工具复用核心能力。PDF、研究数据和 llms 导出有独立脚本，不会因一次普通构建自动全部刷新。

静态架构目前没有显示出必须重构才能增长的证据。真实扩展约束是内容对象较大、路由/元信息映射需手工同步、派生物仍有 4,395 等硬编码、归档将全部历日题目装入单个 HTML、复杂 SVG 使部分页面较重。先解决有增长证据的局部问题，不启动框架迁移或 CMS 重建。

### 日替题、CI 与部署

每日按日本日期提供 7 题（初级 2、中级 2，其余三档各 1），基准日 2026-06-14；构建时嵌入约 60 天窗口，浏览器按日期切换。归档嵌入历史 7 题集合。当前余量检查瓶颈是 hard：**252 天**，高于 90 天阈值；这不意味着未来无需构建。

.github/workflows/ci.yml 在 push/PR 跑类型检查、引擎验证、构建及 postbuild。daily-build.yml 配置每日 UTC 16:00（JST 01:00）执行余量、类型、引擎检查，生成时间数据，有 diff 才 commit/push。**当前时间生成器已不简单按每天推进全部页面时间，因此工作流不保证每天产生提交或部署。** 页面窗口能否持续刷新，需要部署记录和线上嵌入数据监控确认。2026-09-07 的运行 `34155793527` 在 `git push` 因 main 同时有内容提交而被拒绝；9/8 的后续运行已生成 `dca66dd`，证明题库与生成链可用。当前工作区已补上安全重试：每次从远端 main 重取、重新生成后再推送，最多三次；修复尚未提交或部署。

仓库说明与配置指向 Cloudflare Pages Git 集成、项目 numpredo2，GitHub 仓库为 rusherxie-sudo/numpredo。本轮验证了线上服务与本地路由一致，未能读取 Cloudflare 控制台的部署 SHA；不能把 GitHub 成功或线上 HTML 当作 Cloudflare 部署记录的替代。

## SEO 实现与边界

- Base.astro 统一 title、description、canonical、OG/Twitter 和日语页面。结构化数据包含组织/运营者、Article、FAQPage、BreadcrumbList，以及工具、研究等专属类型。标记存在不保证富结果资格或排名收益。
- robots.txt 允许抓取，包含 Sitemap 声明与 AI crawler 规则；llms.txt、llms-full.txt 是辅助分发物，尚无可归因的自然搜索增益证据。
- astro.config.mjs 排除 /stats/ 与未进入白名单的编号题。indexable-puzzles.json 为空，play/[level]/[n].astro 模板仍在，但生成 **0 个编号题页面**。
- public/_redirects 保留旧 React/多语言、旧难度和编号题的相关 301。抽查 /play/advanced/20/、/play/hard/92/、/en/wrong-answer、/zh-TW/printable 均正确 301，随机无效 URL 返回 404；尚未穷举所有历史地址。
- lastmod 按内容 Git 修改时间离线维护，部分数据条目采用 slug 范围归因。但映射不覆盖所有间接依赖，元数据与后续提交可能不同步。例如区别页 lastmod 对应 9/8 前一次修改，尚未包含 HEAD 的最后标题修改；构建通过不代表所有时间已同步到最新源代码。
- 现有校验检查 canonical、Sitemap、日期、结构化数据、标题/描述重复、正文长度、相似度、正文入链及初始 JS。62 页均满足脚本的至少 2 条上下文入链阈值；字数/相似度/链接数只是防退化检查，不能证明满足搜索意图。

线上 HTML 解压后抽样：例题集 616,145 字节，练习 307,039，技巧打印 261,220，归档 185,552，每日题 144,611，求解器 119,008。这些不是压缩网络传输量或 Core Web Vitals。本轮仅对求解器做 390px 浏览器渲染抽查，无横向溢出；未做完整功能、OCR 精度或真实用户性能审计。

## 搜索与使用基线

统一比较窗口：**2026-08-10～09-06**，对照 **2026-07-13～08-09**，均 28 天。GSC 使用 web、dataState=final；站点总量单独查询，页面/关键词明细不作为总量分母。GA4 日期遵循属性时区；各平台日期口径不同，不强行做逐日精确对账。

| 指标 | 本期 | 前期 | 变化 |
| --- | ---: | ---: | ---: |
| Google 自然点击 | 1,908 | 534 | +257.3% |
| Google 曝光 | 56,570 | 14,583 | +287.9% |
| Google CTR | 3.37% | 3.66% | -0.29 个百分点 |
| Google 平均排名 | 8.00 | 9.72 | 改善，受查询构成影响 |
| Bing 点击 | 4,087 | 2,734 | +49.5% |
| Bing 曝光 | 185,904 | 119,759 | +55.2% |
| GA4 Organic Search 会话 | 6,790 | 3,294 | +106.1% |

Google 移动端 1,386 点击（72.6%）、桌面 447、平板 75；日本 1,892 点击。Bing 是重要既有获客来源，不能以只优化 Google 为由牺牲游戏页表现。

GA4 自然搜索用户 3,521、互动会话 4,985，互动会话率 73.4%，自然会话约占所有渠道会话 85.6%；但 645 个自然会话着陆页为 (not set)，占 9.5%。其中复核样本显示，主要是回访用户在长期打开的页面上再次操作触发新会话、却没有新的 page_view，并非可直接归因到某个着陆页。

全站事件：game_start 3,961 次/1,157 用户，game_complete 4,446 次/499 用户；solver_solve 2,803 次/284 用户；solver_ocr 592 次/211 用户；print_pdf_download 468 次/274 用户；mini4_start 7 次/3 用户。这些事件未按自然渠道过滤，不能冒充 SEO 转化数据。

game_start 在每个盘面组件首次交互时只发一次，game_complete 可随同页多次完成题目上报，二者不是同一粒度。GA4 与百度统计都在首次交互或 8 秒后加载，短停留无交互访问仍可能漏记；但现有 `(not set)` 主要机制已确认是回访无新 page_view，不应为补齐维度强行补发 page_view。不能用完成次数/开始次数宣称完成率，也不能把高时长直接解释为体验优秀。

2026-09-10 固定自然渠道任务口径（GA4 时区 Asia/Tokyo，2026-08-12～09-08）：报告“包含至少一次事件的 Organic Search 会话”，并与事件次数、用户数分列。`game_complete` 为 1,709 会话/4,343 次/430 用户，`solver_solve` 为 337/1,758/255，`print_pdf_download` 为 334/532/304；`solver_ocr` 225/491/199，`print_generate` 206/420/180。各事件会话可重叠，不能相加。代码核对未发现错误触发，维持现有埋点；详细字典与复核条件见 [docs/2026-09-10-自然渠道任务衡量口径.md](docs/2026-09-10-自然渠道任务衡量口径.md)。

## 历史决策重新定性

| Git/历史证据 | 当前解释 |
| --- | --- |
| 2026-06-14 62c73da：React/多语言重建为日语 Astro | 当前仓库最早可见提交；旧语言 URL 经重定向保留，不意味应恢复多语言 |
| 6 月扩展游戏、提示、保存、解法与统计 | 已成为产品资产，继续围绕真实任务维护 |
| 7/24、7/28 编号白名单收敛后扩充，8/11 aea27fd 清空 | 曾尝试题目规模化，目前题库价值保留在工具与精选说明；4,395 题不等于 4,395 个 SEO 页面 |
| 9/3～9/7 增加 4×4、16×16 示例、高龄者 PDF、内容验证 | 当前代码已实现，不再列为待开发；新增后的流量效果尚需观察 |
| 9/8 908d551、e2ce2b5、8780f09 连续改区别页标题 | 约 90 分钟内多次变更；历史冻结期失效，需要重建观察窗 |
| 9/5 报告称 blank、algorithm、number-place 未收录 | 本期前两者已有 3/1 次点击；number-place 无曝光，不能只凭此判定当前索引状态 |
| 引擎 README 称待接 Astro、待做 SVG、题库存 R2 | Astro/SVG/本地题库已落地，R2 不是当前依赖，这些待办过时 |

## 验证、来源与未确认事项

本轮 npm run build（含 check:seo、check:value）、npm run check、npm run demo、npm run check:daily 均成功。类型检查仍有已有内联脚本/弃用 API 等提示；不为消提示做无收益重构。引擎验证包含标准 4,395 题、daily 派生溯源、两种 30 题变体和 12 道迷你题。

| 来源 | 获取情况与边界 |
| --- | --- |
| 源码/Git/构建 | 直接核验；关键路径与提交见本文 |
| 线上站点 | 读取全部 62 个 Sitemap URL、robots、部分 301/404；移动渲染仅抽查求解器 |
| GSC | 直接查询总量、143 条页面、1,070 条关键词、1,527 条 page×query、国家、设备及前期页面；未触及行数上限，仍存在匿名查询与聚合差异 |
| GA4 | 直接查询属性 539597439 的渠道、事件和着陆页；前端测量 ID G-CM79TWN2J0 |
| Bing Webmaster | 直接读取 RankAndTraffic、Page、Query、Crawl；站点日数据截止 9/7，统一比较截到 9/6；页面/查询按周标记返回，不当成同窗口日明细 |
| Semrush | 初次直接访问 sem.3ue.co 的路径错误，不能用该超时判断服务可用性。正确流程及 9/9 补查记录见下；尚未获取本轮 JP 搜索量、KD、竞品或外链数据 |
| 索引与外链 | 未取得最新 GSC 全站索引报告/外链导出；Bing 9/8 返回 InIndex=92、Code301=403、Code4xx=1、Code5xx=0；不能拿 92 减 62 推算错误页数 |
| 部署、收益、性能 | 已核查 GitHub Actions 的失败与后续成功；Cloudflare 部署 SHA 仍因无只读 token 未核验。AdSense 审核/收入或 CrUX/CWV 未读取。代码有 AdSense 验证 meta 与 ads.txt，未加载广告脚本，不等于已获批或有收益 |

### Semrush 访问流程纠正（2026-09-09）

用户指定的必经路径：复用已登录的 Codex 独立会话 → 打开 https://dash.3ue.co/ 控制台 → 在节点 3–8 中选择可用节点 → 点击“打开”进入 Semrush。不得直接打开 sem.3ue.co 代替控制台选节点流程。

初次检查没有选择节点，直接访问入口超时，因此不能归因为登录失败或某个节点故障。补查确认控制台已登录，初始显示节点 11；该默认显示不代表初次审计选择过节点 11。补查实际从控制台选择节点 4 后点击进入，随后继续检查节点 3、5、6、7、8。各节点的最终结果以本轮会话记录为准，不能把本机加载异常认定为节点永久不可用。

补查结果：节点 3 最终返回已登录的订阅控制台，没有进入报表；节点 4、5、6、7、8 均跳转到 sem.3ue.co/home/，但本次浏览器会话未能读取页面正文（正文等待超时，节点 4 还出现导航状态异常）。尚不能确定是节点、会话跳转还是浏览器加载问题。未获得可用于本项目研究的 Semrush 报表，不宣称所有节点或整个服务不可用。

原始 API 结果与临时核查脚本仅存本机 /tmp/numpredo-takeover-VLurzq/，未纳入 Git；这是临时证据位置，不保证长期留存。仓库只保留本项目必要汇总。GSC 总量口径参考[官方完整数据说明](https://developers.google.com/webmaster-tools/v1/how-tos/all-your-data)，GA4 请求参考[官方报告说明](https://developers.google.com/analytics/devguides/reporting/data/v1/basics)，本轮通过 Context7 核实。
